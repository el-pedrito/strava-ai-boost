# Coach streaming Lambda (Starlette + Lambda Web Adapter).
# Starlette + uvicorn are pure-Python — they vendor into the asset with no Docker
# bundling (project convention; see README "Known Issue #2").
# Pinned to recent stable versions; run pip-audit after install (security policy §10).
starlette==1.3.1
uvicorn==0.49.0
typing_extensions==4.15.0  # runtime dep of anyio/starlette on py3.12

