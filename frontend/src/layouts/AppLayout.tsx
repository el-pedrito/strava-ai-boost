// Legacy entry — preserved so existing imports keep working.
// New shell lives in `src/components/layout/`.
export { Shell } from '../components/layout/Shell';
export { useFlash } from '../components/layout/flash';
